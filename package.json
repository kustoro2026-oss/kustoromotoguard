{
  "name": "kustoromotoguard",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time IoT motorcycle fleet monitoring & alert system with MQTT, WebSocket, and GPS tracking",
  "workspaces": [
    "packages/backend",
    "packages/simulator",
    "packages/frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w packages/backend",
    "dev:simulator": "npm run dev -w packages/simulator",
    "dev:frontend": "npm run dev -w packages/frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:simulator\" \"npm run dev:frontend\"",
    "build": "npm run build -w packages/backend && npm run build -w packages/frontend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w packages/backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
